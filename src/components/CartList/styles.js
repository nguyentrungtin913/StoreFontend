import { HEIGHT } from './../../constants';

const styles = () => ({
  listProductType: {
    textAlign: 'center',
    fontSize: '16pt'
  },
  myPanelProductType: {
    height: HEIGHT - 150 + 'px',
    overflow: 'scroll',
    overflowX: 'hidden'
  }
});

export default styles;
